import { Observable } from "rxjs";
import { RouteContext } from "../../../router-ex/";
export declare class ItemComponent {
    id$: Observable<string>;
    constructor(routeContext: RouteContext);
}
